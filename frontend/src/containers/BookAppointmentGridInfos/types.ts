import { UserCardInfos } from "~/services/api/CommunicationService/types";

export interface BookAppointmentGridInfosProps {
  userInfos: UserCardInfos;
}

export interface StatusCircleProps {
  isAvailable: boolean;
}
